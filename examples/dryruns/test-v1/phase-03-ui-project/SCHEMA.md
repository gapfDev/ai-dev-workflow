# SCHEMA

*Note: This is a frontend UI project without backend schema defined in this phase. Mock data structures will be used.*

## Mock Data Interfaces
- `SalesData` (date: string, amount: number)
- `User` (id: string, email: string, token: string)
